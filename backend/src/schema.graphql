# import * from './generated/prisma.graphql'

type SuccessMessage {
  message: String
}

type Mutation {
  hi: String
}

type Query {
  hi: String
  habits: [Habit]
}

type User{
  id: ID!
  name: String!
}
